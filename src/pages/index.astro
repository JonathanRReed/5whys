---
import Base from '../layouts/Base.astro';
import ToolCard from '../components/ToolCard.astro';

const metrics = [
  {
    label: 'Deep reflection prompts',
    value: '35+',
  },
  {
    label: 'Resume scoring heuristics',
    value: '18',
  },
  {
    label: 'Guided networking scenarios',
    value: '24',
  },
];
---
<Base
  title="Career Tools"
  description="A modern studio of reflection, practice, and career storytelling tools built on the Rosé Pine palette."
  theme="night"
>
  <div class="relative space-y-24 px-4 pb-32 pt-20 sm:pt-24 lg:space-y-32">
    <section class="mx-auto max-w-6xl">
      <div class="grid gap-16 lg:grid-cols-[minmax(0,1fr)_minmax(320px,420px)] lg:items-center">
        <div class="space-y-10">
          <div class="inline-flex items-center gap-3 rounded-full border border-[hsl(var(--border)/0.5)] bg-[hsl(var(--overlay)/0.4)] px-4 py-2 text-xs font-semibold uppercase tracking-[0.28em] text-muted-foreground">
            Guided Career Studio
            <span class="inline-flex h-2 w-2 rounded-full bg-[hsl(var(--foam))]" />
          </div>

          <div class="space-y-6">
            <h1 class="text-balance text-4xl font-semibold tracking-tight text-foreground sm:text-5xl">
              Modern tools to help you design a career story with intention.
            </h1>
            <p class="max-w-xl text-pretty text-lg leading-relaxed text-muted-foreground">
              Explore reflective exercises, interactive resume polish, and networking practice studios—all crafted to feel calm, cohesive, and focused on your next professional chapter.
            </p>
          </div>

          <div class="flex flex-wrap items-center gap-4">
            <a
              href="/career"
              class="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-6 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))] shadow-[0_20px_40px_-24px_hsl(var(--primary))] transition-transform duration-200 hover:-translate-y-0.5 hover:shadow-[0_26px_52px_-30px_hsl(var(--primary))]"
            >
              Start with 5 Whys
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
              </svg>
            </a>
            <a
              href="#tools"
              class="inline-flex items-center gap-2 rounded-full border border-[hsl(var(--border)/0.55)] px-6 py-3 text-sm font-semibold text-foreground transition-colors duration-200 hover:border-[hsl(var(--accent)/0.4)] hover:text-[hsl(var(--accent))]"
            >
              Browse the toolkit
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4 12h16" />
              </svg>
            </a>
          </div>

          <div class="grid gap-4 sm:grid-cols-3">
            {metrics.map((metric) => (
              <div class="rounded-2xl border border-[hsl(var(--border)/0.4)] bg-[hsl(var(--overlay)/0.35)] p-4">
                <div class="text-2xl font-semibold text-foreground">{metric.value}</div>
                <div class="text-sm text-muted-foreground">{metric.label}</div>
              </div>
            ))}
          </div>
        </div>

        <div class="relative">
          <div class="absolute -inset-8 rounded-[2.75rem] border border-[hsl(var(--border)/0.25)] bg-[hsl(var(--card)/0.6)] blur-sm" aria-hidden="true" />
          <div class="relative space-y-6 rounded-[2.5rem] border border-[hsl(var(--border)/0.45)] bg-[hsl(var(--card)/0.86)] p-10 shadow-[0_30px_80px_-50px_hsl(var(--background)/0.95)]">
            <div class="flex items-center justify-between">
              <span class="text-sm font-semibold uppercase tracking-[0.3em] text-muted-foreground">This week’s focus</span>
              <span class="rounded-full bg-[hsl(var(--foam)/0.2)] px-3 py-1 text-xs font-medium text-[hsl(var(--foam))]">Intention</span>
            </div>
            <p class="text-lg font-medium leading-relaxed text-foreground">
              "The clearer your why, the more courageous your next step becomes."
            </p>
            <div class="space-y-3 text-sm text-muted-foreground">
              <div class="flex items-center gap-3 rounded-2xl border border-[hsl(var(--border)/0.4)] bg-[hsl(var(--overlay)/0.45)] px-4 py-3">
                <svg class="h-5 w-5 text-[hsl(var(--foam))]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M7 8h10M7 12h10m-5 4h5" />
                </svg>
                Journal through five layers of "why" to reach your core motivation.
              </div>
              <div class="flex items-center gap-3 rounded-2xl border border-[hsl(var(--border)/0.4)] bg-[hsl(var(--overlay)/0.45)] px-4 py-3">
                <svg class="h-5 w-5 text-[hsl(var(--gold))]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v12m6-6H6" />
                </svg>
                Capture personal stories that reinforce your next career conversation.
              </div>
            </div>
            <a
              href="/career"
              class="inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-[hsl(var(--primary)/0.14)] px-6 py-3 text-sm font-semibold text-[hsl(var(--primary))] transition hover:bg-[hsl(var(--primary)/0.2)]"
            >
              Continue reflection
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </section>

    <section id="tools" class="mx-auto max-w-6xl space-y-12">
      <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
        <div class="space-y-3">
          <h2 class="text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">Build your toolkit</h2>
          <p class="max-w-2xl text-pretty text-base text-muted-foreground">
            Each experience is crafted to feel calm, focused, and actionable. Choose a tool below to explore guided flows, helpful prompts, and progress you can revisit.
          </p>
        </div>
        <div class="grid gap-8 md:grid-cols-2">
          <ToolCard
            href="/career"
            title="Career Reflection · 5 Whys"
            description="Work through a structured reflection to uncover the root motivations behind your career moves—and translate them into confident language."
            accent="foam"
            cta="Start reflecting"
            tag="Deep dive"
          >
            <svg slot="icon" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v12m6-6H6" />
            </svg>
          </ToolCard>

          <ToolCard
            href="/resume-game"
            title="Resume Momentum Game"
            description="Gamify your bullet points with instant scoring across impact, clarity, and action. Track improvements and save your strongest lines."
            accent="gold"
            cta="Play & improve"
            tag="Momentum"
          >
            <svg slot="icon" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 8v8m-4-4h8" />
            </svg>
          </ToolCard>

          <ToolCard
            href="/networking-practice"
            title="Networking Practice Studio"
            description="Rehearse intros, refine your story, and capture insight after every practice conversation with the guided timer and prompts."
            accent="love"
            cta="Start practicing"
            tag="Rehearsal"
          >
            <svg slot="icon" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
              <path stroke-linecap="round" stroke-linejoin="round" d="M18 8a6 6 0 11-12 0 6 6 0 0112 0zm-6 8c-6.075 0-11 2.686-11 6h22c0-3.314-4.925-6-11-6z" />
            </svg>
          </ToolCard>

          <ToolCard
            href="/role-decoder"
            title="Role Decoder — Design Document"
            description="Offline Astro + vanilla JS web app blueprint for parsing job posts, mapping skills, drafting résumé bullets, and exporting Markdown/JSON without any external APIs."
            accent="iris"
            cta="Review architecture"
            tag="Design doc"
          >
            <svg slot="icon" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h10M4 18h8" />
            </svg>
          </ToolCard>
        </div>
    </section>

    <section class="mx-auto max-w-6xl rounded-3xl border border-[hsl(var(--border)/0.4)] bg-[hsl(var(--overlay)/0.35)] p-10 md:p-14">
      <div class="grid gap-8 md:grid-cols-[minmax(0,1fr)_minmax(260px,1fr)] md:items-center">
        <div class="space-y-4">
          <h3 class="text-2xl font-semibold tracking-tight text-foreground">Designed for calm, confident progress</h3>
          <p class="text-pretty text-base text-muted-foreground">
            Whether you’re deep in reflection or prepping for interviews, the Career Tools studio keeps your focus on what matters. Gentle animations, Rosé Pine colorways, and structured flows help every session feel grounded.
          </p>
        </div>
        <div class="grid gap-3 text-sm text-muted-foreground">
          <div class="flex items-center gap-3 rounded-2xl border border-[hsl(var(--border)/0.35)] bg-[hsl(var(--card)/0.6)] px-4 py-3">
            <span class="h-2.5 w-2.5 rounded-full bg-[hsl(var(--foam))]" />
            Seamless theming across Night, Moon, and Dawn palettes.
          </div>
          <div class="flex items-center gap-3 rounded-2xl border border-[hsl(var(--border)/0.35)] bg-[hsl(var(--card)/0.6)] px-4 py-3">
            <span class="h-2.5 w-2.5 rounded-full bg-[hsl(var(--iris))]" />
            Guided prompts, timers, and feedback loops built for momentum.
          </div>
          <div class="flex items-center gap-3 rounded-2xl border border-[hsl(var(--border)/0.35)] bg-[hsl(var(--card)/0.6)] px-4 py-3">
            <span class="h-2.5 w-2.5 rounded-full bg-[hsl(var(--love))]" />
            Accessible typography, focus states, and keyboard-friendly interactions.
          </div>
        </div>
      </div>
    </section>
  </div>
</Base>
